// This is a boilerplate unit test file to demonstrate testing logic decoupled from request handling
describe('Example Unit Test', () => {
  it('should pass a basic assertion', () => {
    const sum = 1 + 2;
    expect(sum).toBe(3);
  });
});
